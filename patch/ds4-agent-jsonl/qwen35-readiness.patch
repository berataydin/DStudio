--- a/ds4_agent.c
+++ b/ds4_agent.c
@@ -145,6 +145,7 @@
     bool stop;
     bool interrupt;
     bool initialized;
+    bool active; /* Owner retains the session through deferred work and cleanup. */
     bool save_requested;
     bool reset_requested;
     bool reset_running; /* Worker-only: defer power changes while preparing KV. */
@@ -5225,6 +5226,17 @@
 }
 
 static void agent_publish_system_status(agent_worker *w, const char *msg) {
+    if (w->cfg->jsonl) {
+        /* Service feedback is not model prose. Bound this infrequent record
+         * before escaping it, and keep ordinary non-interactive stdout clean. */
+        char escaped[6152], line[6240];
+        if (strnlen(msg, 1025) > 1024) msg = "Runtime notice exceeds its display limit";
+        ds4ui_json_escape(msg, escaped, sizeof escaped);
+        int n = snprintf(line, sizeof line,
+                         "\x1e{\"type\":\"runtime_notice\",\"text\":\"%s\"}\n", escaped);
+        if (n > 0 && (size_t)n < sizeof line) agent_publish(w, line, (size_t)n);
+        return;
+    }
     if (w->cfg->non_interactive) return;
     if (isatty(STDOUT_FILENO)) {
         static const char marker[] = "\x1b[33m✦ \x1b[38;5;218m";
@@ -10265,7 +10277,6 @@
     agent_config *cfg = w->cfg;
     ds4_think_mode think_mode = effective_think_mode(cfg);
     pthread_mutex_lock(&w->mu);
-    w->interrupt = false;
     w->status.error[0] = '\0';
     agent_wake_locked(w);
     pthread_mutex_unlock(&w->mu);
@@ -10750,7 +10761,6 @@
 static int worker_run_raw_prompt(agent_worker *w, const char *user_text) {
     agent_config *cfg = w->cfg;
     pthread_mutex_lock(&w->mu);
-    w->interrupt = false;
     w->status.error[0] = '\0';
     agent_wake_locked(w);
     pthread_mutex_unlock(&w->mu);
@@ -10848,6 +10858,13 @@
     return 0;
 }
 
+/* Caller holds mu. Display status alone does not transfer session ownership. */
+static bool worker_idle_locked(agent_worker *w) {
+    return w->initialized && !w->active && !w->stop && !w->cmd_text &&
+        !w->save_requested && !w->compact_requested && !w->power_requested && !w->reset_requested &&
+        (w->status.state == AGENT_WORKER_IDLE || w->status.state == AGENT_WORKER_ERROR);
+}
+
 static void worker_request_save(agent_worker *w) {
     pthread_mutex_lock(&w->mu);
     w->save_requested = true;
@@ -10964,9 +10981,7 @@
  * durable operation whose successful effects remain even if reset is canceled. */
 static bool worker_request_new_session(agent_worker *w) {
     pthread_mutex_lock(&w->mu);
-    bool ok = w->initialized && !w->stop && !w->cmd_text && !w->reset_requested &&
-        !w->save_requested && !w->compact_requested &&
-        (w->status.state == AGENT_WORKER_IDLE || w->status.state == AGENT_WORKER_ERROR);
+    bool ok = worker_idle_locked(w);
     if (ok) {
         w->reset_requested = true;
         w->status.state = AGENT_WORKER_PREFILL;
@@ -11098,8 +11113,27 @@
 
 /* Worker thread entry point.  The UI thread submits plain user text; this
  * thread owns all DS4 session mutation, tool execution, and compaction. */
+/* Publish readiness only after every accepted/deferred operation has ended.
+ * The condition wait releases mu; no model work runs under synchronization. */
+static bool worker_wait_for_work(agent_worker *w) {
+    pthread_mutex_lock(&w->mu);
+    w->active = false;
+    pthread_cond_broadcast(&w->cond);
+    agent_wake_locked(w);
+    while (!w->stop && !w->cmd_text &&
+           !w->save_requested && !w->compact_requested && !w->power_requested && !w->reset_requested)
+        pthread_cond_wait(&w->cond, &w->mu);
+    bool run = !w->stop;
+    w->active = run;
+    pthread_mutex_unlock(&w->mu);
+    return run;
+}
+
 static void *worker_main(void *arg) {
     agent_worker *w = arg;
+    pthread_mutex_lock(&w->mu);
+    w->active = true;
+    pthread_mutex_unlock(&w->mu);
     agent_trace(w, "agent worker start ctx=%d backend=%s model=%s trace=%s",
                 agent_worker_effective_ctx_size(w),
                 ds4_backend_name(w->cfg->engine.backend),
@@ -11121,11 +11155,8 @@
     agent_wake_locked(w);
     pthread_mutex_unlock(&w->mu);
 
-    while (true) {
+    while (worker_wait_for_work(w)) {
         pthread_mutex_lock(&w->mu);
-        while (!w->stop && !w->cmd_text && !w->save_requested && !w->reset_requested &&
-               !w->compact_requested && !w->power_requested)
-            pthread_cond_wait(&w->cond, &w->mu);
         if (w->stop) {
             pthread_mutex_unlock(&w->mu);
             break;
@@ -11209,10 +11240,13 @@
 /* Submit one user turn if the worker is idle.  Busy submissions are rejected so
  * the UI can keep the typed text editable instead of silently queueing it. */
 static bool worker_submit(agent_worker *w, const char *text) {
+    char *candidate = xstrdup(text); /* Prepare variable-sized input outside mu. */
     pthread_mutex_lock(&w->mu);
-    bool ok = w->initialized && w->status.state == AGENT_WORKER_IDLE && !w->cmd_text;
+    bool ok = worker_idle_locked(w) && w->status.state == AGENT_WORKER_IDLE;
     if (ok) {
-        w->cmd_text = xstrdup(text);
+        w->interrupt = false;
+        w->cmd_text = candidate;
+        candidate = NULL;
         /* A submitted turn is no longer idle, even if the worker thread has
          * not yet reached its real prefill accounting.  Non-interactive mode
          * depends on this to avoid exiting in the small handoff window between
@@ -11229,6 +11263,7 @@
         pthread_cond_signal(&w->cond);
     }
     pthread_mutex_unlock(&w->mu);
+    free(candidate);
     return ok;
 }
 
@@ -11242,6 +11277,8 @@
 static void worker_interrupt(agent_worker *w) {
     pthread_mutex_lock(&w->mu);
     w->interrupt = true;
+    pthread_cond_broadcast(&w->cond);
+    agent_wake_locked(w);
     if (w->cfg &&
         w->cfg->engine.distributed.role == DS4_DISTRIBUTED_COORDINATOR &&
         (w->status.state == AGENT_WORKER_PREFILL ||
@@ -11260,7 +11297,7 @@
 static void worker_stop(agent_worker *w) {
     pthread_mutex_lock(&w->mu);
     w->stop = true;
-    pthread_cond_signal(&w->cond);
+    pthread_cond_broadcast(&w->cond);
     pthread_mutex_unlock(&w->mu);
 }
 
@@ -11294,9 +11331,7 @@
 
 static bool worker_is_idle(agent_worker *w) {
     pthread_mutex_lock(&w->mu);
-    bool idle = w->initialized &&
-        (w->status.state == AGENT_WORKER_IDLE ||
-         w->status.state == AGENT_WORKER_ERROR);
+    bool idle = worker_idle_locked(w);
     pthread_mutex_unlock(&w->mu);
     return idle;
 }
@@ -12851,6 +12886,14 @@
                     printf("save failed: %s\n", ds4ui_err);
                 fflush(stdout);
             }
+            /* Ownership is quiescent, but its final output may have arrived
+             * after the previous poll consumed the stream. Drain it first. */
+            char *ready_output = NULL;
+            size_t ready_length = 0;
+            worker_consume(&worker, &ready_output, &ready_length, NULL);
+            if (ready_length) write_all(STDOUT_FILENO, ready_output, ready_length);
+            free(ready_output);
+            fflush(stdout);
             agent_noninteractive_marker("+DWARFSTAR_WAITING");
             waiting_announced = true;
         }
