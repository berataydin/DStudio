diff --git a/ds4.c b/ds4.c
--- a/ds4.c
+++ b/ds4.c
@@ -60330,3 +60330,3 @@ int ds4_engine_memory_pressure_begin(ds4_engine *e, uint64_t *advised_bytes) {
     fprintf(stderr,
-            "ds4: temporary Qwen memory pressure active backend=%s advised=%.2f GiB configured_ssd_streaming=%s\n",
+            "ds4: temporary media memory pressure active backend=%s advised=%.2f GiB configured_ssd_streaming=%s\n",
             ds4_backend_name(e->backend), (double)total / 1073741824.0,
@@ -60345,3 +60345,3 @@ int ds4_engine_memory_pressure_end(ds4_engine *e) {
     fprintf(stderr,
-            "ds4: temporary Qwen memory pressure released backend=%s configured_ssd_streaming=%s\n",
+            "ds4: temporary media memory pressure released backend=%s configured_ssd_streaming=%s\n",
             ds4_backend_name(e->backend), e->ssd_streaming ? "on" : "off");
diff --git a/ds4.h b/ds4.h
--- a/ds4.h
+++ b/ds4.h
@@ -229,3 +229,3 @@ int ds4_engine_set_power(ds4_engine *e, int power_percent);
 /* Temporarily yield model residency/page-cache to a co-located accelerator
- * pipeline (vision, PDF OCR or image generation). Calls are nestable. This
+ * media-generation pipeline. Calls are nestable. This
  * does not change the engine's configured --ssd-streaming mode. */
diff --git a/ds4_metal.m b/ds4_metal.m
--- a/ds4_metal.m
+++ b/ds4_metal.m
@@ -3645,3 +3645,3 @@ int ds4_gpu_memory_pressure_begin(void) {
     ds4_gpu_stream_expert_cache_clear_all(1);
-    fprintf(stderr, "ds4: Metal model residency suspended for Qwen\n");
+    fprintf(stderr, "ds4: Metal model residency suspended for local media\n");
     return 1;
@@ -3652,3 +3652,3 @@ int ds4_gpu_memory_pressure_end(void) {
     if (!ds4_gpu_model_residency_request_views()) return 0;
-    fprintf(stderr, "ds4: Metal model residency restored after Qwen\n");
+    fprintf(stderr, "ds4: Metal model residency restored after local media\n");
     return 1;
